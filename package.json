{
  "name": "mermaid-flow-animator",
  "version": "0.1.0",
  "description": "Render Mermaid flowcharts as live animated diagrams inside Obsidian — particles flow through pipe-styled edges to visualize success/error paths.",
  "main": "main.js",
  "private": true,
  "scripts": {
    "build": "node esbuild.config.mjs production",
    "dev": "node esbuild.config.mjs",
    "typecheck": "tsc --noEmit",
    "version": "node version-bump.mjs && git add manifest.json versions.json"
  },
  "keywords": [
    "obsidian",
    "obsidian-plugin",
    "mermaid",
    "flowchart",
    "animation",
    "diagram"
  ],
  "author": "VivaldiCode",
  "license": "MIT",
  "dependencies": {
    "dagre": "^0.8.5"
  },
  "devDependencies": {
    "@types/dagre": "^0.7.52",
    "esbuild": "^0.28.0",
    "obsidian": "^1.12.3",
    "typescript": "^5.6.3"
  }
}
